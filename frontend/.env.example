# API Base URL - defaults to Render backend in production
# For development, set to http://localhost:5000
# For production on Netlify, the app will auto-detect and use the Render backend
VITE_API_URL=https://subscriber-increasing-web-backend.onrender.com
